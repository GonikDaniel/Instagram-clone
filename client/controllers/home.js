angular.module('Instagram').controller('HomeCtrl', ['$scope', function($scope){
    
    // check if logged in
    $scope.isAuthenticated = function() {
        
    };

    // connect email account with instagram
    $scope.linkInstagram = function() {
        
    };
}]);